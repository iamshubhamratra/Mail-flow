# Hoist nothing implicitly; keep deps explicit per workspace.
# Allow build scripts for known native/build deps used in this monorepo.
auto-install-peers=true
strict-peer-dependencies=false
# Next.js + shadcn tooling expect a flat-ish layout for some peers
shamefully-hoist=false
