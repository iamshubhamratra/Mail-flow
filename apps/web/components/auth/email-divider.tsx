/** "— OR EMAIL —" mono divider used between OAuth and the email form. */
export function EmailDivider() {
  return (
    <div className="text-muted-2 mono my-5 flex items-center gap-3 text-[11px]">
      <span className="bg-hairline h-px flex-1" />
      OR EMAIL
      <span className="bg-hairline h-px flex-1" />
    </div>
  );
}
